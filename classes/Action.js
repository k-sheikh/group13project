export class Action {

    constructor(name, score, message) {     // Setting properties with variables passed into the constructor
        this.name = name;
        this.score = score;
        this.message = message;
    }
}